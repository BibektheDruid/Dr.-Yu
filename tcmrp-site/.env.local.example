# Contact form storage (reuses the intake-form Vercel Blob pattern).
# Create a Vercel Blob store, Connect Project, then `vercel env pull .env.local`.
BLOB_READ_WRITE_TOKEN=

# Secret for the private submissions viewer at /inbox?key=YOUR_KEY
INBOX_KEY=choose-a-secret
